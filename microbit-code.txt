from microbit import *
import music

uart.init(baudrate=115200)

while True:
    if button_a.was_pressed():
        # This will be sent through serial.
        print("Hello")
        music.pitch(440)
        sleep(250)
        music.stop()

    # Check for incoming data
    if uart.any():
        data = uart.readline().decode().strip()
        display.scroll(data)
        

    
